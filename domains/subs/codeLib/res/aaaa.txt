//make a single time span instead of one per year

public MembershipEnrollment checkEnrollment(List<MembershipEnrollment> mes, LocalDate beg, LocalDate end, LocalDate anchor){
		return checkEnrollment(mes, beg, end, normalMaxGaps, normalMaxGapWidth, anchor, false);
	}
	public MembershipEnrollment checkEnrollment(List<MembershipEnrollment> mes, LocalDate beg, LocalDate end, int maxGaps, int maxDaysInGap, LocalDate anchor, boolean oneChunkEnrollment) {
List<TimeSpan> years
if (oneChunkEnrollment){
years = Tools.listIt(new TimeSpan(beg, end, maxGaps, maxDaysInGap, anchor));
} else {
years = makeYearList(beg, end, maxGaps, maxDaysInGap, anchor);
}   