var methods = {
    GET: function(req, res){
	var path = req.url[0]
	var file = req.url[1]

	var dot = file.lastIndexOf('.')
	var base, xten
	if (dot == -1){
	    base = file
	    xten = ''
	} else {
	    base = file.substring(0, dot)
	    xten = file.substring(dot+1)
	}


	
	if (xten == 'html'){
	    var mold = JSON.parse(fs.readFileSync(path+base+'.json', 'utf8'));
	    console.log(mold)
	    //read the json
	    //build the thing
	    //res.end()
	}},

    POST: function(req, res){
	res.end("bleh")
    }
}
	
module.exports.main = function(req, res){
	console.log("old moldy")
    var func = methods[req.method]
    if (typeof func == 'undefined')
	res.end('404')
    func(req, res)
}
